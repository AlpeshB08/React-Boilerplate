import Layout from '@components/layout';
// import {UseMediaQuery} from '@utils/commonFunctions'

const About = () => {
    // const isMobile = UseMediaQuery('(max-width: 768px)');
    // console.log(isMobile);
    return (
        <>
            <Layout>
                This is About page   
            </Layout>
        </>
    );
};

export default About;